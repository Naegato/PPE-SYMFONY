{% set route = app.request.attributes.get('_route') %}
{% set param = app.request.attributes.get('_route_params') %}

<nav class="navbar navbar-expand-lg navbar-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">
{#            <img src="{{ asset('./img/logo.png') }}">#}
            <div class="logo">Logo</div>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link {% if route == 'index' %}active{% endif %}" aria-current="page" href="{{ path('index') }}">Acceuil</a>
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="#">Biens</a>
                    </li>
                    {% if app.user.roles[0] != 'ROLE_TENANT' %}
                        <li class="nav-item">
                            <a class="nav-link {% if route == 'tenants' %}active{% endif %}" href="{{ path('tenants') }}">Locataire</a>
                        </li>
                        {% if app.user.roles[0] == 'ROLE_ADMINISTRATOR' %}
                            <li class="nav-item">
                                <a class="nav-link {% if route == 'representatives' %}active{% endif %}" href="{{ path('representatives') }}">Mandataire</a>
                            </li>
                        {% endif %}
                    {% endif %}
                {% endif %}
            </ul>
            {% if app.user %}
                <a class="nav-link {% if route == 'userId' and param.id == app.user.id %} active {% endif%} bi bi-person-circle" href="/user/{{ app.user.id }}"> Mon profil</a>
                <a class="nav-link bi bi-box-arrow-right" href="/logout"></a>
            {% endif %}
        </div>
    </div>
</nav>
